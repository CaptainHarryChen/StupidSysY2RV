fun @main(): i32 {
%entry_main:
  @a = alloc i32
  store 2, @a
  %0 = load @a
  br %0, %true, %false

%true:
  %1 = load @a
  %2 = add %1, 1
  store %2, @a
  jump %end

%end:
  %3 = load @a
  ret %3

%false:
  store 0, @a
  jump %end
}
